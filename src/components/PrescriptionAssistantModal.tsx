import React from 'react';

export interface PrescriptionAssistantModalProps {
  isOpen: boolean;
  onClose: () => void;
}

export const PrescriptionAssistantModal: React.FC<PrescriptionAssistantModalProps> = () => {
  return null;
};
